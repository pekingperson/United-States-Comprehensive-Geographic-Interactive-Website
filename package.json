{
  "name": "united-states-comprehensive-geographic-interactive-website",
  "version": "1.0.0",
  "description": "Interactive United States geography viewer combining Census Reporter profiles, Census Dots tiles, Opportunity Atlas overlays, and policy overlays.",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "data": "node --use-system-ca scripts/generate-data.mjs && python scripts/apply-urban-merges.py",
    "deploy": "wrangler deploy",
    "merge-urban-areas": "python scripts/apply-urban-merges.py",
    "preview": "wrangler dev",
    "start": "node --use-system-ca scripts/serve.mjs 4173",
    "serve": "node --use-system-ca scripts/serve.mjs 4173"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
